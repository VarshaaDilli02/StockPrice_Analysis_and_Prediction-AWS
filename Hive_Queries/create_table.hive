create table aws(dte string, open double, high double, low double, close double, adj double, volume double)
row format delimited fields terminated by ',' stored as textfile
tblproperties("skip.header.line.count="1");
